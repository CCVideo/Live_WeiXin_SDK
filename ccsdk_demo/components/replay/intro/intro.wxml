<view class="container">
    <view class="title">
        <text>{{initData.title}}</text>
    </view>
    <view class="content" wx:if="{{initData.intro}}">
        <rich-text nodes="{{initData.intro}}"></rich-text>
    </view>
</view>
